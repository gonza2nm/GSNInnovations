/*
export interface FormData {
  email: string;
  name: string;
  subject: string;
  message: string;
}

export const send = async (formData: FormData) => {
  try {
    //const res = await axios.post();
    //console.log(body);
  } catch (error) {
    console.error('aca tira error: ' + error);
    throw error;
  }
};
*/
